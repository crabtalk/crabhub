[package]
name = "discord"

[services.discord]
description = "Discord bot gateway for agents"
kind = "client"
command = "walrus-discord"
install = { command = "cargo", args = ["install", "walrus-gateway"] }
restart = "on_failure"
config = { discord = { token = "" } }
