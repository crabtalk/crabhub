[package]
name = "telegram"
description = "Telegram bot gateway for agents"
logo = "https://openwalrus.xyz/logos/telegram.png"
repository = "https://github.com/aspect-build/walrus"
keywords = ["telegram", "bot", "gateway", "chat"]

[services.telegram]
description = "Telegram bot gateway for agents"
kind = "client"
command = "walrus-telegram"
install = { command = "bash", args = ["-c", "curl -fsSL openwalrus.xyz/install | sh -s -- --yes --service telegram"] }
restart = "on_failure"
config = { telegram = { token = "" } }
